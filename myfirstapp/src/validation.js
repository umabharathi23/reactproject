export default function Validation5(formData){
    const error={}
  if(FormData.name === " "){
         error.name=" name is required"
  }
  if(FormData.rollno=== " "){
    error.rollno="email is required"
  }
}